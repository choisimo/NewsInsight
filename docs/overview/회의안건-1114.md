# 회의 안건 – 2025-11-14

## 1. Gateway 요청 제한 설정 점검

- **배경**: Gateway가 Redis 설정을 읽어 요청 횟수를 제한합니다.(@backend/api-gateway-service/src/main/resources/application.yml#27-106)
- **확인 질문**
  1. 운영/스테이징마다 Redis 주소와 비밀번호가 최신 값으로 입력되어 있나요?
  2. Redis 연결이 끊기면 어떻게 알 수 있고, 대신 사용할 방법이 준비되어 있나요?
  3. Redis 보안(접속 권한, 암호화)을 강화해야 하는 항목이 있다면 언제까지 개선하나요?

## 2. `force` 옵션 사용 방향

- **배경**: 수집 요청에 `force` 값을 넣을 수 있지만 지금은 동작하지 않습니다.(@backend/data-collection-service/src/main/java/com/newsinsight/collector/dto/CollectionRequest.java#5-13, @backend/data-collection-service/src/main/java/com/newsinsight/collector/service/CollectionService.java#67-176)
- **확인 질문**
  1. 어떤 상황에서 `force`가 꼭 필요할까요? (예: 긴급 소스 재수집)
  2. 누가 이 옵션을 쓸 수 있어야 할까요? (예: 관리자 전용)
  3. 기능을 만들기로 하면 언제까지 누가 반영하고, 기존 호출 방식에 영향은 없나요?

## 3. 데이터 소스 중복 등록 막기

- **배경**: 같은 뉴스 소스를 여러 번 등록하지 않도록 하는 규칙이 없습니다.(@backend/data-collection-service/src/main/java/com/newsinsight/collector/service/DataSourceService.java#77-141, @backend/data-collection-service/src/main/java/com/newsinsight/collector/repository/DataSourceRepository.java#13-29)
- **확인 질문**
  1. 중복을 막을 기준을 URL 하나로 할지, 이름+도메인으로 할지 결정됐나요?
  2. DB에 중복 금지 규칙을 추가하려면 준비해야 할 작업과 일정은 무엇인가요?
  3. 사용자가 중복으로 등록하려 할 때 어떤 안내 문구를 보여줄까요?

## 4. 권한 관리 방식 개선

- **배경**: Gateway에서 역할과 권한 목록이 코드에 고정되어 있습니다.(@backend/api-gateway-service/src/main/java/com/newsinsight/gateway/filter/RbacFilter.java#27-67)
- **확인 질문**
  1. 새로 추가해야 하는 역할이나 권한 요청이 있나요?
  2. 역할·권한 정보를 코드가 아닌 설정 파일이나 DB로 빼낼 계획이 있나요?
  3. 권한을 바꿀 때 테스트와 롤백 절차는 어떻게 준비할까요?

## 5. 수집 품질 지표 활용하기

- **배경**: `CollectedData`에 품질 점수 관련 필드는 있지만 실제로 어떻게 쓰일지 정해지지 않았습니다.(@backend/data-collection-service/src/main/java/com/newsinsight/collector/entity/CollectedData.java#58-82)
- **확인 질문**
  1. 품질 점수는 지금 어디서 계산되고 있나요? 값이 비어 있으면 기본값은 어떻게 되나요?
  2. 점수를 활용해 자동 필터링이나 보고서를 만들 계획이 있나요?
  3. 점수 계산이 실패하면 알림을 보내거나 다시 시도할 방법이 필요할까요?

## 6. API 문서와 테스트 확인

- **배경**: README 문서에 API 설명이 있지만 테스트와 문서 업데이트 규칙은 정리되어 있지 않습니다.
- **확인 질문**
  1. Collector API(예: `/api/v1/collector/*`)를 검증하는 자동 테스트가 있나요?
  2. API가 바뀌면 문서를 언제, 누가 업데이트하는지 정해져 있나요?
  3. 추가로 문서가 필요한 영역(예: Gateway 공개 경로, 통계 API)이 있나요?
